# Frontend Environment Variables

# Base URL pointing to the FastAPI backend API
# Local dev default (via Vite proxy): empty or /api
# Production default (e.g. Railway / Render / Vercel):
VITE_API_URL=https://healthcare-appointment-manager-production-831c.up.railway.app/api
